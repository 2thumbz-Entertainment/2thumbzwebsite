# OS files
.DS_Store
Thumbs.db

# Editor files
.vscode/
.idea/
*.swp
*.swo

# Dependencies
node_modules/

# Build files
dist/
build/

# Environment files
.env
.env.local

# Separate project that happens to live in this folder — it has its own
# git repo, so it must not be committed into the website repo
MailchimpSync/

# Local tooling (dev-server config used to preview this site)
.claude/
